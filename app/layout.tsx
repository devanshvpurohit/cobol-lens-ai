import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CobolLens AI — COBOL Code Intelligence Platform",
  description:
    "Upload COBOL files, visualize program dependencies, get AI-powered explanations, and ask questions about legacy code. Modernize your COBOL understanding.",
  keywords: ["COBOL", "legacy code", "AI", "code analysis", "dependency graph", "modernization"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
